#include <iostream>
#include "Screen.h"
using std::cout;
using std::endl;

/*
* 1. Why do we need inline const Screen& set(const char) const; ? 
*	 Why is that possible at all?
*/
int main(void)
{
	Screen<> s;
	return 0;
}